package com.tried.${systemName}.${packName}.service.impl;

import org.springframework.stereotype.Service;
import com.tried.base.service.impl.BaseServiceImpl;
import com.tried.${systemName}.${packName}.model.${ModelName};
import com.tried.${systemName}.${packName}.service.${ModelName}Service;
/**
 * @Description ${modelTitle} 服务接口实现
 * @author ${author}
 * @date ${date}
 * @version V1.0
 */
@Service
public class ${ModelName}ServiceImpl extends BaseServiceImpl<${ModelName}> implements ${ModelName}Service {

}
