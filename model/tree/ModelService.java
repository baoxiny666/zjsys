package com.tried.${systemName}.${packName}.service;

import com.tried.${systemName}.base.service.BaseService;
import com.tried.${systemName}.${packName}.model.${ModelName};

/**
 * @Description ${modelTitle} 服务接口
 * @author ${author}
 * @date ${date}
 * @version V1.0
 */
public interface ${ModelName}Service extends BaseService<${ModelName}> {

}
